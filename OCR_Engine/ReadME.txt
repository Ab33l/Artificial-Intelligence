The project is aimed at transforming scanned PDF files, that ideally are PDF files but each page appears as a scanned image, to full image 
files that characters can be extracted using OCR.

Requirements: Must be a Windows Machine
              Need to install Pytesseract Engine whic is imported into the Jupyter Notebook

